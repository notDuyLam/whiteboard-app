<Page
    x:Class="whiteboard_app.Views.CanvasManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:whiteboard_app.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Padding="24">
            <TextBlock 
                Text="Canvas Manager"
                Style="{StaticResource TitleTextBlockStyle}"
                FontSize="32" />
        </StackPanel>

        <!-- Canvas List -->
        <ScrollViewer Grid.Row="1" Padding="24">
            <StackPanel Spacing="16">
                <ProgressRing 
                    x:Name="LoadingProgressRing"
                    Height="40"
                    Width="40"
                    HorizontalAlignment="Center"
                    Visibility="Collapsed" />

                <GridView
                    x:Name="CanvasesGridView"
                    SelectionMode="Single"
                    IsItemClickEnabled="True"
                    ItemClick="CanvasesGridView_ItemClick"
                    Visibility="Visible">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                Width="300"
                                Height="200">
                                <StackPanel Spacing="8">
                                    <TextBlock 
                                        Text="{Binding Name}"
                                        FontWeight="SemiBold"
                                        FontSize="18"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock 
                                        Text="{Binding Profile.Name, StringFormat='Profile: {0}'}"
                                        FontSize="14"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock 
                                        Text="{Binding CreatedDate, StringFormat='Created: {0:yyyy-MM-dd HH:mm}'}"
                                        FontSize="12"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                    <TextBlock 
                                        Text="{Binding LastModifiedDate, StringFormat='Modified: {0:yyyy-MM-dd HH:mm}'}"
                                        FontSize="12"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                        <Button
                                            Content="Open"
                                            Click="OpenCanvasButton_Click"
                                            Tag="{Binding}"
                                            Style="{StaticResource AccentButtonStyle}" />
                                        <Button
                                            Content="Delete"
                                            Click="DeleteCanvasButton_Click"
                                            Tag="{Binding}"
                                            Style="{StaticResource DefaultButtonStyle}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>

                <TextBlock
                    x:Name="EmptyStateTextBlock"
                    Text="No canvases available. Create a canvas from the Home page."
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="16"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    Visibility="Collapsed" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

