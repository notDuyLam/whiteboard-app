<Page
    x:Class="whiteboard_app.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:whiteboard_app.Views"
    xmlns:viewmodels="using:whiteboard_app.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:HomeViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <ScrollViewer>
        <StackPanel Spacing="24" Padding="24">
            <!-- Welcome Section -->
            <StackPanel Spacing="12">
                <TextBlock 
                    Text="Welcome to Whiteboard App"
                    Style="{StaticResource TitleTextBlockStyle}"
                    FontSize="36" />
                <TextBlock 
                    Text="Select a profile to start drawing, or create a new one to customize your settings."
                    Style="{StaticResource BodyTextBlockStyle}"
                    TextWrapping="Wrap"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
            </StackPanel>

            <!-- Profile Selection Section -->
            <StackPanel Spacing="16">
                <TextBlock 
                    Text="Available Profiles"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    FontSize="24" />

                <ProgressRing 
                    IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                    Height="40"
                    Width="40"
                    HorizontalAlignment="Center"
                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

                <GridView
                    x:Name="ProfilesGridView"
                    ItemsSource="{x:Bind ViewModel.Profiles, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}"
                    SelectionMode="Single"
                    IsItemClickEnabled="True"
                    ItemClick="ProfilesGridView_ItemClick"
                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                MinWidth="200"
                                MinHeight="120">
                                <StackPanel Spacing="8">
                                    <TextBlock 
                                        Text="{Binding Name}"
                                        Style="{StaticResource SubtitleTextBlockStyle}"
                                        FontWeight="SemiBold" />
                                    <TextBlock 
                                        Text="{Binding Theme}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                    <TextBlock 
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                                        <Run Text="Canvas: " />
                                        <Run Text="{Binding DefaultCanvasWidth}" />
                                        <Run Text="x" />
                                        <Run Text="{Binding DefaultCanvasHeight}" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button
                    x:Name="StartDrawingButton"
                    Click="StartDrawingButton_Click"
                    IsEnabled="{x:Bind ViewModel.SelectedProfile, Mode=OneWay}"
                    Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE70F;" FontSize="16" />
                        <TextBlock Text="Start Drawing" />
                    </StackPanel>
                </Button>
                <Button
                    Click="CreateProfileButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="16" />
                        <TextBlock Text="Create New Profile" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>

