<Page
    x:Class="whiteboard_app.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:whiteboard_app.Views"
    xmlns:viewmodels="using:whiteboard_app.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Padding="24">
            <TextBlock 
                Text="Profile Management"
                Style="{StaticResource TitleTextBlockStyle}"
                FontSize="32" />
            <Button
                Click="CreateProfileButton_Click"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16" />
                    <TextBlock Text="Create Profile" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Profile List -->
        <ScrollViewer Grid.Row="1" Padding="24">
            <StackPanel Spacing="16">
                <ProgressRing 
                    x:Name="LoadingProgressRing"
                    Height="40"
                    Width="40"
                    HorizontalAlignment="Center" />

                <GridView
                    x:Name="ProfilesGridView"
                    SelectionMode="Single"
                    IsItemClickEnabled="True"
                    ItemClick="ProfilesGridView_ItemClick">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                MinWidth="250"
                                MinHeight="150">
                                <StackPanel Spacing="8">
                                    <TextBlock 
                                        Text="{Binding Name}"
                                        Style="{StaticResource SubtitleTextBlockStyle}"
                                        FontWeight="SemiBold" />
                                    <TextBlock 
                                        Text="{Binding Theme}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                    <TextBlock 
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                                        <Run Text="Canvas: " />
                                        <Run Text="{Binding DefaultCanvasWidth}" />
                                        <Run Text="x" />
                                        <Run Text="{Binding DefaultCanvasHeight}" />
                                    </TextBlock>
                                    <TextBlock 
                                        Text="{Binding DefaultStrokeColor}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                    <Button
                                        x:Name="EditProfileButton"
                                        Content="Edit"
                                        Click="EditProfileButton_Click"
                                        Tag="{Binding}"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Margin="0,8,0,0" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

